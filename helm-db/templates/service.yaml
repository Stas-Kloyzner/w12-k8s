apiVersion: v1
kind: Service
metadata:
  name: {{ .values.service.name }} #postgresdb
spec:
  type: {{ .values.service.type }} #ClusterIP
  ports:
  - port: {{ .values.service.ports.port }} #5432
    targetPort: {{ .values.service.ports.targetPort }} #5432
  selector:
    app: {{ .values.service.selector.app}} #postgresdb


# apiVersion: v1
# kind: Service
# metadata:
#   name: {{ include "helm-db.fullname" . }}
#   labels:
#     {{- include "helm-db.labels" . | nindent 4 }}
# spec:
#   type: {{ .Values.service.type }}
#   ports:
#     - port: {{ .Values.service.port }}
#       targetPort: http
#       protocol: TCP
#       name: http
#   selector:
#     {{- include "helm-db.selectorLabels" . | nindent 4 }}
